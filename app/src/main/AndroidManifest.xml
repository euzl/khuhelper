<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dasom.khuhelper">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
<!--        User-->
        <activity android:name=".user.UserActivity" />
        <activity android:name=".user.SearchActivity" />
        <activity android:name=".user.PlaceActivity" />
        <activity android:name=".user.PetitionActivity" />

<!--        Admin-->
        <activity android:name=".admin.AdminActivity" />
        <activity android:name=".admin.OptimalPlaceActivity" />
        <activity android:name=".admin.PredictActivity" />
        <activity android:name=".admin.ManageActivity" />

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="589cb9e85b062d763feef79e05c544f1"/>
    </application>

</manifest>